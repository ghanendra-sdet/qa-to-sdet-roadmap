# Workspace Sync Test

This is a temporary file created to verify that local edits → git commit → GitHub push
(and the reverse: removal → commit → push) are working correctly after the repo/folder
rename from `QA-to-SEDT-Roadmap` to `SDET-Roadmap`.

Safe to ignore — this file will be deleted in the next commit.
